import { BASE_URL } from "./constants";
import axios from "axios";

export async function seedLanguages() {
  // const response = await axios.post(`${BASE_URL}/api/v1/languages`, {
  //   name: "English",
  //   code: "EN"
  // });
  alert(response);
}
